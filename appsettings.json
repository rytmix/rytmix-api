{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "//": "No real connection string committed. Set locally in appsettings.Development.json (gitignored) or user-secrets; on Render set env var ConnectionStrings__DefaultConnection.",
    "DefaultConnection": ""
  },
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:3000"
    ]
  },
  "Jamendo": {
    "//": "ClientId is a SECRET — never commit the real value. Set it locally in appsettings.Development.json (gitignored) or user-secrets; on Render set env var Jamendo__ClientId.",
    "ClientId": "",
    "BaseUrl": "https://api.jamendo.com/v3.0/"
  }
}
